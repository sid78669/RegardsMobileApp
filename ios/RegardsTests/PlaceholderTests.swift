import Testing
@testable import Regards

struct PlaceholderTests {
    @Test("Phase 0 scaffold builds and the test harness runs")
    func scaffoldBuilds() {
        #expect(Bool(false), "VERIFICATION ROUND-TRIP — proves Unit tests fails CI now (will be reverted)")
    }
}
